CREATE TABLE `hb_student_tracker`.`instructor_detail` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `youtube_chanel` VARCHAR(128) NULL DEFAULT NULL,
  `hobby` VARCHAR(45) NULL DEFAULT NULL,
  PRIMARY KEY (`id`));
